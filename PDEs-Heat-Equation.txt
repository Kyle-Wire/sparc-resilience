Lecture Notes on PDEs, part I: The heat equation and the eigenfunction method 
Fall 2018 
Contents 
1 
Motivating 
example: 
Heat 
conduction 
in 
a 
metal 
bar 
2 
2 
The 
heat 
equation: 
preliminaries 
5 
2.1 
Initialandboundary 
conditions 
......................... 6 
2.2 
Linearity 
andhomogeneousPDEs........................ 7 
2.3 
Moreonsuperposition 
.............................. 9 
3 
Orthogonal 
functions 
and 
L2 
11 
3.1 
Linear 
algebra 
analogy: 
using 
orthogonal 
bases 
in 
Rn 
............. 11 
3.2 
Whatistheright 
spaceoffunctions? 
...................... 12 
3.3 
Someexamples;importance 
oftheinterval 
. . . . . . . . . . . . . . . . . . . 14 
4 
Eigenfunctions 
15 
4.1 
Solvingtheeigenvalue 
problem 
......................... 16 
4.2 
Eigenfunctionsandorthogonalbases 
...................... 18 
4.3 
Example:Neumannboundary 
conditions 
.................... 19 
5 
The 
eigenfunction 
method 
to 
solve 
PDEs 
21 
5.1 
Themethod 
(fortheheatequation) 
....................... 21 
5.2 
Dirichlet 
boundary 
conditions 
.......................... 23 
5.3 
Neumannboundary 
conditions.......................... 25 
5.4 
Anexample 
.................................... 26 
5.5 
Anotherexample: 
.nitenumber 
ofmodes 
. . . . . . . . . . . . . . . . . . . 29 
5.5.1 
Thee.cient 
approach: 
.......................... 30 
6 
Separation 
of 
variables 
32 
6.1 
A.rstexample 
.................................. 32 
6.2 
Themethod 
.................................... 33 
6.3 
Useinsolvingunfamiliarproblems 
....................... 35 
6.3.1 
Example: 
................................. 35 
7 
Appendix: 
additional 
notes 
36 
7.1 
Somecontext: 
PDEsfromconservation 
laws 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
. 
36 

7.2 Derivingtheheatequation............................ 37 
Notation: Hereafter, subscripts will denote derivatives with respect to a variable. For instance, 
..2u 
ut(x, t)= (u(x, t)),uxx = , etc. 
.t .x2 
A partial di.erential equation (PDE) for a function of more than one variable is a an equation involving a function of two or more variables and its partial derivatives. 


1 Motivating example: Heat conduction in a metal bar 
A metal bar with length L = p is initially heated to a temperature of u0(x). The temper­ature distribution in the bar is u(x, t). At the ends, it is exposed to air; the temperature outside is constant, so we require that u = 0 at the endpoints of the bar. 
Over time, we expect the heat to di.use or be lost to the environment until the temperature of the bar is in equilibrium with the air (u . 0). 

Physicist Joseph Fourier, around 1800, studied this problem and in doing so drew attention to a novel technique that has since become one of the cornerstones of applied mathematics. The approach outlined below hints at some of the deep structure we will uncover in the remainder of the course. 
The temperature is modeled by the heat equation (see subsection 
7.1 
for a derivation) 
.u .2u 
= , t> 0 and x . (0,p). 
.t .x2 
Since the temperature is .xed at both ends, we have u(0,t)=0,u(p, t)=0 for all t. Lastly, the initial heat distribution is t =0 is u(x, 0) = f(x) 
where f(x) is some positive function that is zero at 0 and p. The temperature should de­crease as heat leaks out of the bar through the ends; eventually it all dissipates. The solution u(x, t) should predict this. 
In summary, our goal is to .nd a function u(x, t) de.ned on [0,p] satisfying 
.u .2u 
= t> 0 and x . (0,p), (1a)
.t .x2 u(0,t)= u(p, t)=0 for t = 0 (1b) u(x, 0) = u0(x). (1c) 
Our objective here is just to .nd a solution to the .rst two parts, 
(1a) 
and (1b) 
and worry about the initial condition later. 
First, let us guess a solution of the form -.tf(x).
u = e (2) Substituting into the PDE (1a), 
we .nd that 
-.f(x)= f00(x). Now substitute into the boundary conditions (1b) 
(note 
that e-.t cancels out here) to get f(0) = 0,f(p)=0. For convenience set . = µ2 . It follows that (2), 
our guess for u, satis.es the PDE (1a) 
and 
the boundary conditions (1b) 
if the function g(x) solves the boundary value problem f00(x)+ µ 2f(x)=0,f(0) = 0,f(p)=0. (3) This problem is not an initial value problem (conditions are imposed at both ends), but it is a constant-coe.cient ODE, so we can still solve it explicitly. The general solution is f = c1 sin(µx)+ c2 cos(µx). Imposing the condition f(0) = 0 we .nd that f = c1 sin(µx). The second condition, f(1) = 0, requires that sin(µp)=0. 
Non-trivial solutions exist whenever µ is a non-zero integer. We have now found an in.nite sequence of solutions to (3): 

fn(x) = sin(nx),n =1, 2, 3, ··· 
Observe that (3) 
is a linear, homogeneous problem. In particular, 
f1,f2 are solutions to (3) 
=. c1f + c2f2 is a solution. (4) 
This means that for any constant an, the function 
ane -n2tfn(x) (5) 
is a solution to the heat conduction problem with initial data 
u0(x)= an sin(nx). 
Now the crucial question: what happens when the initial data is not a sine? No single solution of 
the 
form 
(5) 
will 
work. Fourier’s breakthrough was the realization that, using the superposition principle 
(12), 
the solution could be written as an in.nite linear combination of all the solutions of the form (5): 

8
X 
u(x, t)= ane -n2tfn(x). n=1 
Then u(x, t) solves the original 
problem 
(10) 
if the coe.cients an satisfy 
8
X 
u0(x)= anfn(x). (6) n=1 
This idea is a generalization of what you know from linear algebra (representing vectors in terms of a basis) but with basis functions {sin(nx): n =1, 2, 3, ···}. 
In fact, this set of functions has the rather remarkable orthogonality property 
Z
Z pp fm(x)fn(x) dx = sin(mx) sin(nx) dx =0,m 6= n. (7) 
00 
To solve for the coe.cient am, we can multiply 
(6) 
by sin(mx) and integrate: 
ZZ 8
pp X 
u0(x) sin(mx) dx = an sin(mx) sin(nx) dx. 00 
n=1 
Now move the integral inside the sum (it is not trivial to show this is allowed!). By the property (7), 
only 
one of the terms in the sum will be non-zero: 
ZZ 8
pp X 
u0(x) sin(mx) dx = an sin(mx) sin(nx) dx 00 
n=1 
8 Z
X p = an sin(mx) sin(nx) dx 
0
n=1 ! 8 Z
X p = an · 0+ am sin(mx) sin(mx) dx 
0 
n=1,n6=m
Z p 
= am sin2(mx) dx. 0 
Magically, the in.nite sum has been reduced to a simple equation for am: 
Rp 
u0(x) sin(mx) dx 
am = 0 Rp . (8) 
0 sin2(mx) dx 
This process works for all m, so the solution to the heat conduction 
problem 
(5) 
with 
arbitrary 
initial condition u0(x) is 
8
X 
-n 
u(x, t)= ane 2t sin(nx) n=1 
with the coe.cients given by the formula (8). 
Of course, all of the manipulations here are formal and unjusti.ed -it is far from clear whether the series converges, or if it is valid to swap integrals and sums, and so on (Fourier did not know this either when .rst applying the method; it took several decades to settle the issue). 

2 The heat equation: preliminaries 
Let [a, b] be a bounded interval. Here we consider the PDE 
ut = uxx,x . (a, b),t> 0. (9) 
for u(x, t). This is the heat equation in the interval [a, b]. 
Remark (adding a coe.cient): More generally, we could consider 
ut = kuxx 
where k> 0 is a ’di.usion coe.cient’. However, since the constant can be scaled out by de.ning a rescaled time t = t/k to get 
ut = uxx 
there is no loss of generality in studying the structure of (10). 
Note that it is essential that the coe.cient is positive; a negative k will produce drastically di.erent results. 
2.1 Initial and boundary conditions 
An initial boundary value problem (IBVP) for the heat equation consists of the PDE itself plus three other conditions speci.ed at x = a, x = b and t = 0. As a simple example: 
.u .2u 
= t> 0 and x . (a, b), (10a)
.t .x2 u(a, t)=0 and u(b, t)=0 for t> 0 (10b) u(x, 0) = f(x). (10c) 
There are three components: 
The PDE: Equation (10a) 
is the PDE (sometimes just ’the equation’), which thThe be solution must satisfy in the entire domain (x . (a, b) and t> 0 here). 
Boundary conditions (BCs): Equations 
(10b) 
are the boundary conditions, imposed at the boundary of the domain (but not the boundary in t at t = 0). Each boundary condi­tion is some condition on u evaluated at the boundary. 
Initial conditions (ICs): Equation (10c) 
is the initial condition, which speci.es the initial values of u (at the initial time t = 0). 
The initial boundary value problem 
(10a)-(10c) has a unique solution provided some tech­nical conditions hold on the boundary conditions. 
One can think of the ‘boundary’ of the solution domain to have three sides: {x = a}, {x = b}and {t =0}, with the last side left open (the solution .lls this in as t .8). The initial condition is really a boundary condition at t = 0.1 


De.nition (important BCs): There are three basic types of boundary conditions. Most of the time, we will consider one of these when solving PDEs. 
Dirichlet u(a, t) = 0 (or ’zero boundary conditions’) Neumann ux(a, t) = 0 (or ’zero .ux’) Robin aux(a, t)+ ßu(a, t) = 0 (or ’radiation’) 
1The three-sided boundary is called the parabolic boundary of the IBVP. 
The heat equation could have di.erent types of boundary conditions at a and b, e.g. 
ut = auxx,x . [0, 1],t> 0 u(0,t)=0,ux(1,t)=0 
has a Dirichlet BC at x = 0 and Neumann BC at x = 1. 
Modeling context: For the heat equation ut = auxx, these have physical meaning. Recall that u is the temperature and -aux is the heat .ux. Dirichlet The temperature u is .xed at the end. Neumann The end is insulated (no heat enters or escapes). Radiation Some heat enters or escapes, with an amount proportional to the temperature: 
-aux = ßu. For the interval [a, b] whether heat enters or escapes the system depends on the endpoint and ß. The heat .ux -aux is to the right if it is positive, so at the left boundary a, heat enters the system when ß> 0 and leaves when ß< 0. 
Similarly, at the right boundary b, heat enters the system when ß< 0 and leaves 
when ß> 0. The same interpretations apply when the equation is describing di.usion of some other quantity (e.g. di.usion of a chemical in a tube). 
2.2 Linearity and homogeneous PDEs The de.nitions of linear and homogeneous extend to PDEs. We call a PDE for u(x, t) linear if it can be written in the form L[u]= f(x, t) where f is some function and L is a linear operator involving the partial derivatives of u. Recall that linear means that L[c1u1 + c2u2]= c1L[u1]+ c2L[u2]. The PDE is homogeneous if f = 0 (so l[u] = 0) and inhomogeneous if f is non-zero. Some examples of linear PDEs we will study are 
ut = uxx + g(x, t)(L[u]= ut - uxx), utt = uxx + g(x, t)(L[u]= utt - uxx), 
uxx + uyy = g(x, y)(L[u]= uxx + uyy = r 2 u), 
which are the heat equation, wave equation and the Poisson equation, respectively. Note that the function is u(x, y) in the last one. An example of a non-linear PDE would be 
ut + uux = uxx The same de.nitions apply to boundary conditions. All the boundary conditions listed in the previous section are linear homogeneous. For example, 
ux(a, t) = 0 (11) is a linear boundary condition since if u and v satisfy 
(11) 
and w = c1u + c2v then w also satis.es (11) 
since wx(a, t)= c1ux(a, t)+ c2vx(a, t)=0. Non-homogeneous boundary conditions can be imposed, for instance u(a, t)= t which might be used to model the ambient temperature increasing with time. 
Key fact: A linear, homogeneous PDE obeys the superposition principle: 
u1,u2 are solutions =. c1u1 + c2u2 is a solution (12) for all scalars c1,c2 . R. The same de.nition applies to boundary conditions. For instance, all the boundary conditions listed above are linear homogeneous. 
Note that an inhomogeneous PDE does not have this property! However, the ’homoge­neous’ part (i.e. the equation with the inhomogeneous term set to zero) does, and we will .nd that, as with ODEs, superposition will still be useful. 

2.3 More on superposition 
The superposition principle 
(12) 
is a crucial feature of linear homogeneous problems. Note that while this property is true for homogeneous PDEs and boundary conditions, it is not quite true when initial conditions are included. If u and v are both solutions to the homo­geneous problem 
ut = uxx, t> 0 and x . (a, b), u(a, t)=u(b, t)=0 for t> 0 
where u has initial condition 
u(x, 0) = f1(x) and v has initial condition 
v(x, 0) = f2(x) then w = u + v solves the IBVP 
wt = wxx, t> 0 and x . (a, b), w(a, t)=w(b, t)=0 for t> 0 w(x, 0) = f1(x)+ f2(x). Superimposing two solutions to the PDE with BCs will give another solution, and the initial conditions get superimposed. 
We can exploit superposition to split a problem into simpler parts. For example, suppose we seek u solving the inhomogeneous problem 
ut = uxx + h(x, t), u(0,t)=u(1,t)=0, u(x, 0) = f(x) with non-zero initial conditions. We can split this into u = v + w where v solves an inhomogeneous problem with zero initial conditions, 
vt = vxx + h(x, t), v(0,t)=v(1,t)=0, v(x, 0) = 0 and w(x, t) solves a homogeneous problem with non-zero initial conditions, 
wt = wxx, w(0,t)=w(1,t)=0, w(x, 0) = f(x), thereby splitting the problem for u into two simpler parts. 
To check this, plug v + w into the PDE, boundary conditions and initial conditions and use linearity. For the PDE, we check that 
ut =(v + w)t = vt + wt = vxx + wxx + h(x, t)= uxx + h(x, t). 
For the boundary at x =0, we have u(0,t)= v(0,t)+ w(0,t)=0 and similarly u(1,t) = 0. Finally, for the initial condition, u(x, 0) = v(x, 0) + w(x, 0) = 0+ f(x)= f(x). 


3 Orthogonal functions and L2 
Before proceeding to the PDEs, we need to identify the right space in which to consider our solutions and extend notions of orthogonality and so on from linear algebra in Rn to this space. The main goal here is to extend the notion of an orthogonal basis of eigenvectors in 
Rn 
to a space of functions. 
3.1 Linear algebra analogy: using orthogonal bases in Rn Recall that two vectors x and y are called orthogonal if their dot product is zero: 
x · y = 0 where x · y = x1y1 + ··· + xnyn. (13) 
An orthogonal basis for Rn is a basis b1, ··· bn such that the basis vectors are all pairwise orthogonal, i.e. 
bi · bj =0,i =6 j. 
An orthogonal basis is convenient because it means that the representation of a vector x . Rn in terms of the basis is easy to .nd. 
(Important!) Solving (in Rn) for the coe.cients: Suppose x . Rn and {b1, ··· , bn} is an orthogonal basis for Rn . We know that there are unique coe.cients a1, ··· an such that 
n
X 
x = ajbj. j=1 
Now consider a single index i. To .nd the i-th coe.cient ai, we can take the dot product of both sides with vi. The dot product vi · (··· ) sends all the components except vi to zero: 
n
X 
bi · x = aj(bi · bj)= aibi · bi j=1 
so 
bi · x 
ai = . (14)bi · bi 
A key result in linear algebra is the following: 
Theorem (spectral theorem; symmetric matrices): Let A be an n × n real symmet­ric matrix. The eigenvalues .1, ··· ,.n of A are real and distinct, and the corresponding eigenvectors v1, ··· , vn form an orthogonal basis for Rn . 
Now suppose we have an n × n matrix A and wish to solve 
Ax = b. (15) 
Assume A is invertible and real symmetric. Using an orthogonal basis of eigenvectors, we can turn the system Ax = b into n independent equations that are trivial to solve. 
Using the theorem, there is an orthogonal basis v1, ··· , vn for Rn of eigenvectors of A, which satisfy Avi = .ivi, for i =1, ··· ,n and vi · vj =0, for i =6 j. 
Write b in terms of this basis (using the boxed method above): 
n b · vi 
X 
b = divi,di = . (16) vi · vi
i=1 
Now we know that x (the unknown) also has a representation in the basis, 
n
X 
x = civi (17) i=1 
where the ci’s are the coe.cients we need to .nd. Plug 
(16) 
and (17) 
into the system 
(15) 
and use that Avi = .ivi to get 
nn
XX 
.icivi = divi. 
i=1 i=1 
Since the vi’s form a basis, it must be that the coe.cients of each term on the left and right hand side are equal, so the above implies that 
ci = di/.i, for i =1, ··· n. 
The solution to (15) 
is therefore 
X
n 1 b · vi 
x = civi,ci = . .i vi · vi
i=1 
3.2 What is the right space of functions? 
We need to de.ne a notion of orthogonality for functions. Let us consider real functions de.ned on an interval [a, b]. De.ne the inner product 
Z b 
hf, gi = f(x)g(x) dx (18) 
a 
can call two functions f, g orthogonal on [a, b] if 
hf, gi =0. 
Analogous to the Euclidean norm for a vector x . Rn , 
q 
kxk2 = x12 + ··· + xn 2 = hx, xi1/2 
we de.ne the L2 norm 
Z b 1/2 
kfk2 = |f(x)|2 dx . 
a 
Finally, we de.ne the relevant space of functions: 
De.nition (L2 spaces) L2[a, b]= {f :[a, b] . R such that kfk2 < 8} or equivalently, the set of functions f on [a, b] such that the integral of its square is .nite, 
Z b 
|f(x)|2 dx < 8. 
a 
The space L2[a, b] is a vector space and the inner product 
Z b 
hf, gi = f(x)g(x) dx 
a 
is well-de.ned for all f, g . L2[a, b]. 
The L2 norm gives us a way to measure distance between two functions. The expression 
Z b 
kf - gk2 = |f(x) - g(x)|2 dx (19)
2 
a 
is a sort of weighted measure of the area between the curves f(x) and g(x) on the inteval [a, b]. This is analogous to the Euclidean distance for vectors: kx - yk2 =(x1 - y1)2 + ··· +(xn - yn)2 
which is the actual distance in Rn between the points at x and y. The quantity (19) 
is sometimes called the mean-square distance or mean-square er­ror if g is some approximation to f. 
Warning (complex functions): All the de.nitions here are true only for real-valued functions. For complex-valued functions, the inner product is instead 
Z b 
hf, gi = f(x)g(x) dx 
a 
where g(x) is thet complex conjugate of g(x). Most of the theory is the same, other than the occasional conjugate. 

3.3 Some examples; importance of the interval 
For functions f and g, notions like orthogonality depend on the underlying space where the functions live. For instance, consider 
f(x)=1,g(x) = cos x. 
Regarded as functions in L2[0,p], the two functions are orthogonal: 
Z p 
p 
hf, gi = cos x dx = sin x =0. 
0
0 
However, as functions in L2[0, p/2] the two functions are not orthogonal, since then 
Z p/2 
hf, gi = cos x dx =1. 
0 
The functions are orthogonal on [0,p] but not on [0, p/2]; the domain matters because the de.nition of the inner product is di.erent for each. 
Another example: Consider the space L2[-1, 1]. We have that 
Z 1 
h1,xi = x dx =0 
-1 
so the constant function 1 and x are orthogonal on [-1, 1]. However, 
Z 1 h1,x 2i = x 2 dx =2 ,
3
-1 
so 1 and x2 are not orthogonal. On the other hand, for g(x)= x2 - 1/3, 
Z 1 
h1,gi =(x 2 - 1/3) dx =2 - 2 =0. 
33
-1 
This means that the set {1, x, x 2 - 1/3} 
is an orthogonal set in L2[-1, 1], whereas {1, x, x2} is not. The process, incidentally, can be continued to generate an orthogonal sequence of polynomials. 


4 Eigenfunctions 
Let us return to the heat equation in a bounded domain with Dirichlet boundary conditions: 
ut = uxx,x . (a, b), t> 0 (20) u(a, t)= u(b, t)=0, t> 0. (21) 
Now write the PDE in the form ut = -L[u], (22) 
where L is the linear operator L[u]= -uxx 
or, in operator notation, 
.2 L = - . 
.x2 
Now let us regard L as an operator acting on functions f(x) (just functions of x), i.e. 
d2 L[f]= f00 
or L = - . 
dx2 
De.nition (eigenfunction): We say that f is an eigenfunction of 
the 
problem 
(20) 
with 
boundary conditions (21) 
if it solves the eigenvalue problem 
L[f]= .f, f(a)=0,f(b) = 0 (23) 
for some . . R (the ’eigenvalue’). Equivalently, we say that f is an eigenfunction of the operator L on [a, b] with boundary conditions f(a)=0,f(b) = 0. 
Note: the boundary conditions (21) 
can be replaced with some other conditions; the de.nition is the same. The boundary conditions for f are the result of plugging u = f(x) into the boundary conditions for u. 
Important note: To have an eigenfunction of the operator L, we must prescribe an interval [a, b] and associated boundary conditions. 
For instance, for the problem 
ut = uxx,x . (0,p) t> 0, 
u(0,t)= ux(p, t)=0, t> 0, 
the eigenvalue problem is 
-f00 
= .f, f0(0) = f0(p)=0 
and we say f is an eigenfunction for L = -d2/dx2 on [0,p] with Neumann boundary condi­tions (or, explicitly, ‘with boundary conditions f0(0) = f0(p) = 0’). 
Notice that the eigenvalue problem is an ODE, so we are really studying a type of ODE problem and making use of it (later) to solve PDEs. In the following section, we forget about the PDE part for now and .nd some eigenfunctions in typical cases. 
Notation (why the negative sign?): The negative sign is just convention and is not necessary; we could instead write 
.2 
ut = L[u],L = . 
.x2 
The ODE for the eigenvalue problem is then 
f00 
= .f 
which is the same as the eigenvalue problem for L = -.2/.x2 , with the sign of . reversed since 
-f00 
=(-.)f. 
Since . is an unknown anyway, it does not matter; we will just get .’s that di.er by a negative sign and the eigenfunctions will be the same. 
The reason for using the negative sign is that it tends to make most, if not all, the eigenvalues positive (rather than mostly/all negative); see examples below. 
4.1 Solving the eigenvalue problem 
Consider the operator 
d2 L = - . 
dx2 The eigenvalue problem for L in any interval with any boundary conditions is straightforward to solve, since we can .nd the general solution exactly. First, let us consider the problem 
Lf = .f, f(0) = f(1)=0 (24) 
i.e. Dirichlet boundary conditions in the interval [0, 1]. The [problem is 
- f00 
= .f, f(0) = f(1) = 0. (25) 
The characteristic polynomial for the ODE is 
p(r)= r 2 + .. 
The roots are 
v r = ±-.. 
There are three cases to consider, as the form of the solution depends on the roots of p. 
v 
Case 1 (.< 0): If . is negative, then the two roots ±-. are both real and distinct. it follows that the general solution for the ODE is 
vv 
-.x -.x
f(x)= c1e + c2e. 
v 
For convenience, set µ = -. (not necessary, but saves space), so that 
µx -µx
f = c1e + c2e. 
We want values of µ> 0 such that f solves (25). 
Now impose the boundary conditions. First, 0= f(0) = c1 + c2 =. c1 = -c2, 
so µx - e -µx).
f = c1(e Now impose the boundary condition at x = 1: 0= f(1) = c1(eµ - e -µ). If c1 = 0 then c2 = 0 which makes the solution trivial. So we only have a (non-trivial) solution if the term in parentheses is zero, which simpli.es to 
2µ
e =1. 
(··· )
But e = 1 only if the exponent is 0, so this requires µ = 0, which is impossible because 
v 
µ = -. and . was assume to be negative. 
Case 2 (. = 0): In this case the ODE is f00 
=0 whose solution is f = c1x + c2. Imposing the boundary conditions, we .nd that f(0)=0 =. c2 =0,f(1)=0 =. c1 + c2 =0 so there are no non-trivial solutions. 
Case 3 (.> 0): Now there are solutions! The roots of the characteristic polynomial are vv r = ±-. = ±i .. 
v 
Let µ = .. Then the general solution to the ODE is f = c1 sin(µx)+ c2 cos µx. Imposing f(0) = 0, we .nd that 0= f(0) = µc2 =. c2 =0 
so f = c1 sin(µx). Imposing the other boundary condition, we .nd that f is a solution to 
(25) 
with .> 0 if and only if 
sin µ =0. 22p2
This has solutions for µ = np (n =1, 2, ··· ), so . = µ = n . We therefore have eigenfunctions/eigenvalues 
2p2
fn = sin(npx),.n = n, for n =1, 2, ··· . 
Notes on the mechanics: Some points to note when computing eigenfunctions: 
• 
If f is an eigenfunction so is any scalar multiple since eigenvlaue problems are always linear homogeneous. We always end up multiplying by an arbitrary constant later, so it does not matter which multiple you choose (e.g. sin(npx) or 2 sin(npx). 

• 
There are sometimes reasons to choose a particular scale for each fn so that it has some nice property (we’ll use this for Fourier series later). 


vv 
• 
Setting . = µ2 is just to avoid writing -. or .. 

• 
The cases are usually but not always . positive/zero/negative. 



4.2 Eigenfunctions and orthogonal bases 
The process of .nding eigenfunctions for the operator Lf = f00 is similar for any of the linear homogeneous BCs -the three standard types we de.ned earlier. All of them (at an endpoint 
a) have the form af(a)+ ßf0(a) = 0 (26) 
e,g. Dirichlet means ß = 0 and Neumann means a = 0. 
It turns out, rather miraculously, that the eigenfunctions we produce are -under suitable technical conditions -an orthogonal basis for a certain space. One version of the theorem2 
is as follows (we’ll generalize a bit later). 
Informally, it says that nice eigenvalue problems will give an orthogonal basis for functions that satisfy the boundary conditions. 
2Note: the theorem is a consequence of a spectral theorem for L2-like spaces, a rather deep result in analysis/linear algebra that generalizes of the spectral theorem for symmetric matrices of subsection 
3.1. 
As it is well beyond the scope of the course, you will have to take this result on faith. 
Sturm-Liouville Theory, simple version: Consider the operator 
d2 
L = - 
dx2 in an interval [a, b] and the eigenvalue problem 
Lf = .f, with BCs of the form (26) 
at x = a and x = b. (27) 
Then the set of eigenfunctions {fn} solving 
(27) 
forms an orthogonal basis for the space 
{f . L2[a, b]: f satis.es the BCs }. 
Moreover, it is true that 
i) There is one eigenfunction for each eigenvalue, 
ii) There are in.nitely many positive eigenvalues that increase to 8, 
iii) There are .nitely many negative eigenvalues. 
We call the set of eigenvalues the spectrum of the operator L (in the interval [a, b] with the associated BCs). 
The last three points just con.rm what we observed in the examples (we will see an example with negative eigenvalues later!). The key result is that the eigenfunctions form an orthogonal basis, which is what we will use in practice to solve PDEs (for now, the other points are just reassurance, as we will typically solve the eigenvalue problem explicitly anyway). 

4.3 Example: Neumann boundary conditions 
Now we solve the problem with Neumann boundary conditions: 
Lf = .f, f0(0) = f0(1)=0 (28) where L = -d2/dx2 as before. The process is the same, and in this case, the general solutions are also the same (see previous example). 
Case 1 (.< 0): The general solution is 
µx µx
f(x)= c1e + c2e 
v 
where µ = -.. We want values of µ> 0 such that f solves (28). 
First, 
0= f0(0) = µc1 - µc2 =. c1 = c2, so 
µx -µx).
f = c1(e + e 
Now drop the c1 (since it multiplies the whole thing, it does not matter) and impose the boundary condition at x = 1: 0= f0(1) = eµ + e -µ. Since both eµ and e-µ are positive, there are no solutions. 
Case 2 (. = 0): Di.erent from the previous example! We have f00 
=0 =. f = c1x + c2. Imposing the boundary conditions, we .nd that f0(0)=0 =. c1 =0,f0(1)=0 =. c1 =0 so there is a non-trivial solution f = const.. A .at line of any value has zero derivative at the boundaries. Let .0 = 0 be the eignvalue; the eigenfunction is then f0 =1 (or any other constant we like). 
v 
Case 3 (.> 0): Set µ = .. The general solution to the ODE is f = c1 sin(µx)+ c2 cos µx. Imposing f0(0) = 0, we .nd that 0= f0(0) = µc1 =. c1 =0 so f = c2 cos µx. (29) Imposing the other boundary condition, we get 0= f0(1) = -c2µ sin µ Since µ =6 0 by assumption, this has solutions for µ = np (n =1, 2, ··· ). This gives eigenvalues . = µ2 and eigenfunctions 
(29): 
2p2
fn = cos(npx),.n = n, for n =1, 2, ··· . The full set of eigenvalues and eigenvectors (collecting all the cases) is 
2p2
.0 =0,f0 =1, and .n = n ,fn = cos(npx) for n =1, 2, ··· . 
Further notes on mechanics (indexing conventions): For some problems, there are positive eigenvalues, a zero eigenvalue, and possibly negative eigenvalues. In the one above, we had a zero eigenvalue and set 
.0 =0,f0 =1. 
Typically, the zero index is saved for the zero eigenvalue (if it exists). 
With negative eigenvalues, one scheme is to label with negative indices. For instance, if there are eigenvalues 
0 <.1 <.2 < ··· and two negative eigenvalues 
then one could label them as 
.-2 <.-1 < 0 with eigenfunctions f-2 and f-1. Or, one could write them as 
a1,a2 with eigenfunctions .1,.2 
or just di.erent labels a and ß. Of course, one could also just relabel indices and label all of them .1,.2, ··· but this tends to mean we lose nice formulas like sin(npx). 
What matters is that we can get the full set of eigenvalues and eigenfunctions; label­ing is just a nuisance. 


5 The eigenfunction method to solve PDEs 
5.1 The method (for the heat equation) 
We are now ready to demonstrate how to use the components derived thus far to solve the heat equation (and by extension, related PDEs). Consider an initial boundary value problem of the form 
ut = -L[u]+ h(x, t),x . (a, b), t> 0 (30) 
with homogeneous BCs at a and b (one of the standard ones) and initial condition 
u(x, 0) = f(x). 
First step: .nd the eigenfunctions. The eigenfunctions we need are the solutions to the eigenvalue problem 
L[f]= .f, f satis.es the BCs for u. (31) 
By an appeal to the theorem in subsection 
4.2, 
there is a sequence of eigenfunctions {fn}with eigenvalues {.n} that form an orthogonal basis for the space 
V = {f . L2[a, b]: f satis.es the BCs}. 
Now at each .xed time t, the function u(x, t), regarded as a function of x, lies inside the space V . It follows that there are coe.cients an(t) such that 
8
X 
u(x, t)= an(t)fn(x). (32) n=0 
For each t,{an(t)} is the set of coe.cients for expressing u(x, t) in terms of the basis {fn}. 
Second step: get everything in terms of the basis, substitute into the PDE: 
Our objective now is to determine the functions an(t). First, we write the source h in terms of the eigenfunctions: 
8
X 
h(x, t)= hn(t)fn(x). n=0 
Now substitute this and the eigenfunction 
expansion 
(32) 
for u into the PDE (30) 
to obtain 
88 8
XX X 
a 0 n(t)fn = - an(t).nfn(x)+ hn(t)fn(x) . (33) n=0 n=0 n=0
|{z}| {z }| {z } 
ut -L[u] h 
In detail, the second term was found using the eigenfunction property and linearity of L: 
"# 
8
X 
L[u]= Lan(t)fn(x) n=0 
8
X 
= an(t)L[fn(x)] n=0 
8
X 
= - an(t).nfn(x). n=0 
Note that since an(t) is only a function of t, it is constant as far as L is concerned so by linearity, L[an(t)fn(x)] = an(t)L[fn(x)]. where we have used the fact that L is linear to move 
Third step: rearrange to get a single sum: Now collect all the terms in (33) 
together: 

8
X 
0= (a 0 n(t)+ .nan(t) - hn(t))fn(x). n=0 
Since the fn’s are a basis, the coe.cient of each basis function must be zero at all times t (otherwise, the fn’s would be linearly dependent at some t). It follows that for each n, 
a 0 n(t)+ .nan(t)= hn(t) for t> 0. (34) 
This equation is a .rst-order linear ODE for an(t) that is easy to solve. If we want the ’general’ solution to the PDE with BCs, then we solve for the an(t)’s and are done. To solve 
the IBVP, the last missing piece is the initial condition -the value of an(0). 
Find the coe.cients at the initial time: To .nd an(0), write the initial condition f(x) in terms of the eigenfunction basis: 
8
X 
f(x)= fnfn(x). n=0 
For the solution u(x, t) 
(32) 
to satisfy the initial condition, we need for constants .n. Then it follows that 
88
XX 
an(0)fn(x)= fnfn(x) . n=0 n=0
| {z }|{z} 
u(x,0) f(x) Again, since the fn’s are absis, the two sums must be equal term-by-term, so 
an(0) = fn. 
Finally, this condition and (34) 
lets us solve for a unique an(t) (as we get a .rst order IVP), which completes the process. 

5.2 Dirichlet boundary conditions 
The simplest case. As an example, suppose we want to .nd the solution u(x, t) to ut = uxx,x . (0, 1),t> 0 (35) with boundary and initial conditions u(0,t)=0,u(1,t)=0,u(x, 0) = f(x). (36) The eigenvalues/eigenfunctions are (as calculated in subsection 
4.1) 

2p2
.n = n ,fn = sin npx, n = 1. Assuming the solution exists, it can be written in the eigenfunction basis as 
8
X 
u(x, t)= an(t)fn(x). n=0 
for unknown coe.cients cn(t) (which vary with time). Substitute into the 
PDE 
(35) 
and 
use the fact that -f00 n = .nf to obtain 
8
X 
(an 0 (t)+ .nan(t))fn(x)=0. n=0 
Equating coe.cients of each basis function (to zero), we .nd that an 0 (t)+ .nan(t)=0. (37) 
The solution is 
an(t)= an(0)e -.nt . To get an(0), write f(x) also in terms of the eigenfunctions: 
8
X 
f(x)= bn sin npx. n=0 Then u(x, 0) = f(x) holds if an(0) = bn. To compute bn, take the inner product of each side with sin mpx to get 
R1 Z 
f(x) sin npx dx 1 
bn = 0R =2 f(x) sin npx dx. (38) 01 sin2 npx dx 0 
The solution to the problem 
(35),(36) is 
8
X 
u(x, t)= bne -n2p2t sin npx with bn given by (40). (39) n=1 
Important special case: If, for example, u(x, 0) = f(x) = sin px + 2sin2px then its expansion in the eigenfunction basis, 
8
X 
sin px + 2sin2px = bn sin npx n=1 
is trivial to .nd as it is already in the form of a linear combination of basis functions; the coe.cient b1 of sin px is 1, the coe.cient of sin 2px is 2 and all the others are zero. 
It follows that only the n = 1 and n = 2 terms of the solution series (39) 
are non-zero; the solution is u(x, t)= e -p2t sin px +2e -4p2t sin 2px. 
Note that this means that given the IBVP with (??) as initial conditions, after .nding the eigenfunctions/values we could have just assumed the solution had the form 
u(x, t)= a1(t) sin px + a2(t) sin 2px 
and then substituted into the ODE, because we know only these two terms will be non-zero in the end (see homework for further details). 
Long-time behavior: Note that every term in 
the 
solution 
(39) 
has 
a negative exponential (since all the eigenvalues are positive). Furthermore, terms further down in the series decay much faster since .n grows quadratically with n. It follows (omitting issues with the in.nite series) that 
lim u(x, t)=0 
t.8 independent of the initial condition f(x) (which just a.ects the bn’s and not the eigenvalues. 
More strongly, we can approximate u by its .rst term and conclude that 
u(x, t) decays to zero at least as fast as Ce-.1t . 
The smallest eigenvalue determines how slow the solution can decay to zero. 
If the eigenfunction expansion of f(x) has in.nitely many terms, we are stuck with an in.nite series solution. For example, suppose f(x)= x(1 - x). After some laborious integration by parts, we get 
( 
2(1 - (-1)n)0 n even 
bn = = (40)
p33 2
nn odd.
p33
n 
The .rst few terms of the solution are 22
-p2 -4p2 
u(x, t)= e t sin px + e t sin 2px + ··· 
p3 27p3 

5.3 Neumann boundary conditions 
A variation -similar to Dirichlet, but with a crucial di.erence due to the zero eigenvalue. Here we seek a solution u(x, t) to the IBVP ut = uxx,x . (0, 1),t> 0 (41) with boundary and initial conditions ux(0,t)=0,ux(1,t)=0,u(x, 0) = f(x). (42) The eigenvalues/eigenfunctions are (as calculated in subsection 
4.3) 

.n = n 2p2 ,fn = cos npx, n =0, 1, 2, ··· Note that .n = 0 is an eigenvalue, unlike the previous case. Regardless, the process is the same and we end up with a solution (check this!) 
8
X 
u(x, t)= ane -n2p2t cos npx n=0 
for constants an determined by the initial condition: 
8
X 
an cos npx = f(x). n=0 
Using the orthogonality of the eigenfunctions on [0, 1], we get 
R
1 Z 
0 f(x) cos npx dx 1 
an = R1 =2 f(x) cos npx dx, n = 1. 
0 cos2 npx dx 0 
To .nd a0, note that the eigenfunction f0 = 1 is orthogonal to the others. Thus, multiplying by f0 = 1 and integrating from 0 to 1 we get Z 1 a0 = f(x) dx. 0 
Convergence as t .8: The zero eigenvalue is signi.cant; note that the solution has the form 
8
X 
u(x, t)= a0 + ane -n2p2t cos npx = a0 + exp. decaying terms. n=1 
As t .8, the solution will approach the constant a0 (again at least as fast as the .rst term in the series, i.e. ~ e-.1t). The constant a0 is the average value of the initial distribution f(x). 
This result con.rms the intuitive notion that if you put something that di.uses into a closed container (e.g. tea in water), then over time the concentration of stu. will even out until it is uniform. 

5.4 An example 
To give a more concrete example: suppose we have a mug of water and place a teabag in it, then let it sit. We want to know how long it will take for the tea to di.use through so that it is uniformly mixed. Assume the mug is one dimensional with height 2 and let c(x, t) be the concentration of tea at height x and time t. 
The concentration obeys the heat (’di.usion’ in this case) equation 
ct = kcxx,x . [0, 1], t> 0 (43) 
where k> 0 is the di.usivity of the tea. Since tea cannot leave the cup, the .ux at the top and bottom must be zero, so impose Neumann boundary conditions 
cx(0,t)= cx(1,t)=0, t> 0. 
Suppose the teabag starts at the bottom of the cup (x = 0), and the initial concentration is 
( 
1 x< 1/2 
c(x, 0) = f(x) := . 
0 x> 1/2 
The eigenvalues/eigenfunctions: It is easiest to write 
.2 
ct = -kL[c],L = - 
.x2 
rather than putting k in the de.nition of L (but that works too). The eigenvalue problem is -f00 
= .f, f0(0) = f0(1) = 0. This is solved as in subsection 
5.3 
to obtain .0 =0,f0 =1 
and3 
.n =(pn)2 ,fn = cos(pnx),n =1, 2, ··· . 
Eigenfunction expansion and coe.cients for the solution: Now that we have a basis, we can write the solution c(x, t) in the form 
8
X 
c(x, t)= an(t)fn(x). n=0 
Plug this into the PDE (43) 
and use the property that -f00 n = .nfn to get 
8 88
X XX 
0 (t)f00 
a (t)fn(x)= kann = -kan fn
n .n n=0 n=0 n=0 
and then gather into a single sum: 
8
X 
(a 0 n(t)+ k.nan(t))fn(x)=0. n=0 
We conclude that an 0 (t)+ k.nan(t)=0,n = 0. 
For n = 0 the eigenvalue is just .0 =0 so 
a 0 0 =0 =. a0(t)= b0 
for a constant b0. For the others, use an integrating factor ek.nt to get -k.nt 
an(t)= bne for arbitrary constants bn. The solution to the PDE with the boundary conditions is then 
8
X 
-k.ntfn
c(x, t)= b0 + bne (x). n=1 
3If you had taken L = k.2/.x2 instead, then the result would be .n = k(pn)2 with the same eigenfunc­tions since the eigenvalue problem is then -f00 =(./k)f. 
Initial condition: The coe.cients are chosen to satisfy the initial condition: 
8
X 
f(x)= b0 + bnfn(x). n=1 
The eigenfunctions are orthogonal on [0, 2]; the inner product is 
Z 2 
hf, gi = f(x)g(x) dx. 
0 
Note that Z 2 
hfn,fni = cos 2(npx) dx = 1 for n = 1 
0 
so 
Z 1 Z 1/2 
1/2
hf, fni 11 
bn == f(x) cos(npx) dx = cos(npx) dx = sin(npx) = sin(np/2). 
hfn,fni 00 np 0 np 
for n = 1 and (taking the inner product with f0 = 1) RZ 1
1 f(x) dx 
b0 = 0R1 = f(x) dx =1/2. 1 dx 0
0 
To summarize, the solution is 
8
1 X -k.ntfn
c(x, t)= + bne (x)
2 
n=1 
where .n =(np)2 ,fn = cos(npx) 
and the coe.cients are given as above. The .rst few coe.cients are b1 =1/p, b2 = 0 and b3 = -1/3p, so 
11 1 
c(x, t)= + e -kp2t cos(px/2) - e -k(3p)2 cos(3px)+ ··· . 
2 p 3p 
Analysis: Since .n > 0 for n = 1, all the modes decay except the zero mode (the b0 term) so it follows that 
1 
lim c(x, t)= b0 = . 
t.8 2 
The value the the limit is the average value of f(x) in the domain. The amount of tea is constant, so as it di.uses, it converges to a uniform concentration equal to the average in the cup. 
The rate of convergence is determined by the .rst term; we have 
1 
-kp2t 
max |c(x, t) - 1/2|~ e 
x.[0,1] p 
so the (exponential) rate of convergence is kp2 . 

5.5 Another example: .nite number of modes 
Here, we use three methods of increasing simplicity to solve 
ut =2tuxx + e -t2 sin x, x . (0,p), t> 0 u(0,t)= u(p, t)=0, t> 0 (44) u(x, 0) = 3sin2x 
by exploiting superposition. 
Eigenfunctions: The 2t goes outside the operator L: ut = -2tL[u]+ h(x, t),h = e -t sin x where 
.2 L = - 
.x2 as before. Note that we cannot put t in the de.nition of L, since L must only involve derivatives in x. The eigenvalues/functions are 
.n = n 2 ,fn = sin(nx),n =1, 2, ··· 
as found before. 
Excessive method: 
We could use the general process described in subsection 
5.1, 
starting with 
8
X 
u(x, t)= an(t)fn(x) n=1 
and writing 
8
X 
h(x, t)= hn(t)fn(x), n=1 
then substituting into the PDE and so on. In this case, the full series is not necessary! 
More e.cient approach (but still excessive): 
Observe that h(x, t) only has an n = 1 mode (just a sin x term). Explicitly, 
8
X 
h(x, t)= hn(t)fn(x) where h1(t)= e -t and hn(t) = 0 otherwise . n=1 
The initial condition only has an n = 2 mode. It follows from the ’general’ process in subsection 
5.1 
than the solution only has these two modes. Thus, we know the solution to the IBVP has the form 
u(x, t)= a1(t) sin x + a2(t) sin 2x. 
The rest of the steps are the same as the general case, but with only two terms instead of the whole sum. 
Equations for the coe.cients: Plug into the PDE: 
00 -t2 
a1(t) sin x + a2(t) sin 2x = -2ta1(t) sin x - 2ta2(t) sin 2x + e sin x. This gives 
0-t2 0
(a1(t)+2ta1(t) - e ) sin x +(a2(t)+2ta2(t)) sin 2x =0. Thus 
0-t2 -t2 -t2 
a1 +2ta1 = e =. a1 = b1e + te using the integrating factor et2 . For the second mode, 0-2t 
a2 +2a2 =0 =. a2 = b2e. Initial condition: Plugging u(x, t) into the initial condition, we get 3 sin 2x = u(x, 0) = a1(0) sin x + a2(0) sin 2x = b1 sin x + b2 sin 2x so b1 =0,b2 =3. The solution is therefore 
-t2 -t2 
u(x, t)= te sin x +3e sin 2x. 
Only the n = 1 mode is a.ected by the inhomogeneous term; only the n = 2 term is a.ected by the initial condition. 
5.5.1 The e.cient approach: Note that each mode evolves independently, so we could solve for each term on its own and then add them together using superposition. The solution has the form u = u1 + u2 where u1 and u2 are the n = 1 and n = 2 modes. We know that u1 is the solution to the full problem (46) 
with all the terms dropped ex­cept the n = 1 mode: ut =2tuxx + e -t2 sin x, x . (0,p), t> 0 u(0,t)= u(p, t)=0, t> 0 (45) u(x, 0) = 0 
The solution to this IBVP has only one term, so we can assume that u1(x, t)= a1(t)f1(x). 
Now this form is quite easy to plug into the PDE; we get 
0-t2 
a1(t)f1 = -2ta1.1f1 + ef1 
which gives 
0-t2 
a1(t)+2ta1 - e =0. Plugging into the initial condition, we get a1(0) = 0; solving then gives a1(t) and 
u1(x, t)= te-t2 sin x. Similarly, the full problem (??) with only the n = 2 mode: ut =2tuxx +0,x . (0,p), t> 0 u(0,t)= u(p, t)=0, t> 0 (46) u(x, 0) = 3sin2x which has a solution u2(x, t)= a2(t) sin 2x. Again, plug in to get a0 2 +2ta2 = 0 and a2(0) = 3 and solve to get 
u2(x, t)=3e -t2 sin 2x. The full solution to the original IBVP is then the sum of solutions for each mode, u = u1 + u2 = 
Exploiting independence of modes: The process here illustrates that we can solve for each mode independently in the following way: 
• 
Find the eigenvalues and eigenfunctions. 

• 
Expand the inhomogeneous term and initial condition in the eigenfunction basis. 

• 
Solve the problem with only one mode to get a solution un(x, t). 

• 
Sum them up to get the full solution. 


The full solution is the superposition of the solutions for each mode.. When we use the eigenfunction method in subsection 
5.1, 
we are assembling the series from the start, 
X 
u = an(t)fn, 
then substituting into the PDE to get equations for all of the modes at once. However, they do not interact with each other, so equations like 
X 
u =(an 0 (t)+ .nan(t))fn =0 
are really the same as saying that for each n, 
un(x, t)= an(t)fn(x) is a solution if (a 0 n + .n(t)an)fn =0. 
It is often easier computationally to compute them one at a time and sum at the end. 


6 Separation of variables 
For homogeneous problems, we can exploit this independence to obtain solutions quickly. Not that what follows is a useful computational trick, and is justi.ed because of the theoretical framework of eigenfunctions already developed. 
6.1 A .rst example 
Consider the equation 
ut = uxx,x . [0,p],t> 0 u(0,t)= u(p, t)=0,t> 0 u(x, 0) = f(x). 
We know that the solution will be an in.nite sum of terms (modes) of a certain form. Let us guess a separated solution 
u(x, t)= F (t)G(x). 
Plug into the PDE to get 
F 0(t)G(x)= F (t)G00(x). 
Now separate variables, putting all the x’s on one side: G00(x)
F 0(t) 
= . 
F (t) G(x) 
But the left hand side is a function of t and the right hand side is a function of x, so for them to be equal, they must both equal a constant: 
G00(x)
F 0(t) 
== -.. 
F (t) G(x) 
This gives the ODEs F 0(t)= -.F (t),G00(x)= -.G(x). 
Plugging this into the boundary conditions, we .nd that 
F (t)G(0) = F (t)G(p)=0 for all t 
so we should require G(0) = G(p)=0. The problem for G is then G00 
+ .G =0,G(0) = G(p)=0 
which is exactly the eigenvalue problem. It has solutions 
Gn = sin nx, .n = n 2 for n =1, 2, ··· . 
Now we solve for F : 
Fn = bne -.nt for constants bn. We have therefore found solutions 
un(x, t)= bne -.nt sin nx. The solution to the PDE plus boundary conditions is then the superposition of all these terms: 
8
X 
u(x, t)= un(x, t) n=1 which is the result we found before. The coe.cients bn are found in the same way. 
What we have done here is solved for each mode on its own (see homework). Note that if you know the eigenfunctions in advance, you could even guess 
un(x, t)= Fn(t) sin nx 
in the .rst place instead of solving for G, saving even more time. 
Note that separation of variables does not work for the example in subsection 
5.5, 
be­cause if we plug in F (t)G(x) into 
-t2 
ut = uxx + e sin x 
then the x and t cannot be separated. Thus we need to identify the eigenfunctions .rst and then guess the solution as done in the example. 

6.2 The method 
Practical note: When it works (for homogeneous problems), separation of variables is the easiest way to solve PDEs. Often, the strategy for more complicated problems is to reduce them to simpler ones where separation of variables applies. 
For some non-homogeneous problems, eigenfunction expansions are necessary, but even then some separation-of-variables inspired tricks will make the computations easier. 
The method of separation of variables is straightforward. Suppose we have some PDE in the variables x and t with boundary conditions. We .nd the ’general’ solution to the PDE with the BCs (leaving initial conditions out) with the following steps: 
• 
Guess a separated solution, a product of functions of each variable: u(x, t)= F (t)G(x). 

• 
Plug into the PDE and separate the t’s and x’s to get 

function of t = function of x. Conclude that both are equal to a constant: function of t = function of x = -.. Note: If this step fails, we must use another method. The negative sign is not needed since . is arbitrary; it is there to match earlier notation. 

• 
Plug into the boundary conditions to get boundary conditions for one of the ODEs. 

• 
Solve the ODE that has the boundary conditions to get eigenvalues/eigenfunctions. Then solve the other ODE to get solutions un(x, t). 

• 
Assume the general solution is an in.nite linear combination: 


X 
u = bnun(x, t). n 
• Solve for the constants using the initial condition for the PDE. Separation of variables, when it works, will produce the correct eigenvalue problem au­tomatically, so it eliminates the need to identify the right operator and reduces the whole process to a concrete series of steps. For instance, consider ut = uxx + ux +(t + 1)u, u(0,t)=0,u(1,t)+ ux(1,t)=0 Note that this PDE is linear and homogeneous. Substitute in u = F (t)G(x) to get F 0(t)G(x)= F (t)G00(x)+ F (t)G0(x)+(t + 1)F (t)G(x). 
Divide by G(x) and F (t) to get: G00
F 0(t)+ G0 
= +(t + 1). 
F (t) G 
Subtract t + 1 and then set both to a constant: G00 + G0
F 0(t) 
- (t +1) = = -.. 
F (t) G Now plug into the BCs: F (t)G(0) = 0,F (t)(G(1) + G0(1)) = 0 so the eigenvalue problem is 
G00 
+ G0 = -.G, G(0) = G(1) + G0(1) = 0. Note that this means the operator L from the eigenfunction method is L[u]= uxx + ux (the (t + 1)u part has to be left out). The ODE for F is 
F 0 +(. + t + 1)F =0. With some work, one can then .nd the eigenvalues/functions .n and Gn, then Fn, and get
X 
u = Fn(t)Gn(x). 
n 

6.3 Use in solving unfamiliar problems 
Separation of variables is particularly useful on more complicated homogeneous problems where the place to start may not be so clear. 
Word of caution (which ODE is the eigenvalue problem?): Separation of variables provides a set of ODEs, but we need to solve them in the right order. 
In the examples below, note that we need to identify which ODE gives the eigen­functions and which ODE does not. Doing so requires understanding the boundary and initial conditions, and requires some domain and equation speci.c intuition. 
Wave equation Consider the wave equation in [0,p] for u(x, t) with Neumann boundary conditions, utt = c 2 uxx,ux(0,t)= ux(p, t)=0 with initial conditions u(x, 0) = f(x),ut(x, 0) = g(x). We substitute u = F (t)G(x) into the PDE to get 
F 00 G00
1 
== -. 
c2 FG 
and into the BCs to get F (t)G0(0) = F (t)G0(p)=0 =. G0(0) = G0(p)=0 which gives the eigenvalue problem 
G00 
+ .G =0,G0(0) = G0(p)=0. The other ODE is 
F 00 
+ c 2.F =0. 
6.3.1 Example: 
consider Laplace’s equation in a disk of radius a for u(r, .), which is 11 
urr + ur + u.. = 0 for . . [0, 2p],r<a 
rr2 
with a boundary condition on the boundary of the disk, u(a, .)= f(.). 
Assume a separated solution 
u(r, .)= R(r)G(.) 
and substitute into the PDE: 
11 
R00G + 
RG00
R0G + 2 =0. 
rr 
Divide by RG to get 
G00
R00 + R0/r 1 
+ =0 
2
R rG 
and then move terms around to separate the r and .; we end up with 
R00 + R0/r G00 -r 2 == -.. 
RG so the ODEs for R and G are G00 2R00
+ .G =0,r + rR0 - .R =0 
Note that plugging into the boundary condition does not give us anything since we would need 
R(a)G(.)= f(.) 
which is too much to ask for G(.) (since f(.) will not be a solution to the ODE unless it is an eigenfunction). We will address the missing boundary conditions for G (and R) when solving this equation later. 



7 Appendix: additional notes 
7.1 Some context: PDEs from conservation laws 
Rather than pull the equation out of thin air, let’s see how PDEs arise naturally out of fundamental models4 
. To do so, we introduce the concept of a conservation law, which is a way of stating that for an amount of stu. in a region, the change in the amount is due to stu. entering/exiting the region or being created/destroyed. For simplicity, assume the stu. is ‘heat’ -but this argument is quite general (e.g. could be particle concentration, momentum, energy, density of .sh, etc.) 

4Adapted from Applied Partial Di.erential Equations, J. David Logan 
Consider a cylindrical tube with cross section A running along the x-direction and u(x, t) the temperature at position x and time t. The amount of heat in a section of the tube for x in some interval [a, b] is 
Z b 
u(x, t) A dx. 
a 
Let us further suppose there is a source g(x, t) that is the rate at which u is created or destroyed at position x along the tube. For instance, heat could leak out of the pipe at a rate g(x, t) if the pipe is poorly insulated. 
De.ne F (x, t) to be the .ux of heat: the rate at which heat .ows through the cross section at x, with units of heat per (area)(time). Thus fA dt is the amount of heat passing through the cross section in a time dt (with sign determining the direction). We have 
. Z b Z b u(x, t)A dx = AF (a, t) - AF (b, t)+ g(x, t)Adx . 
.t | {z }
aa
| {z } heat entering the section from the ends | {z } change in heat heat created/lost due to source 
Cancel out A and move the derivative on the LHS inside the integral, leading to 
Z b Z b 
ut(x, t) dx = F (a, t) - F (b, t)+ g(x, t) dx, aa 
which is a mathematical description of the conservation of heat. 
Now write the F terms in an integral using the Fundamental Theorem of Calculus and collect all the terms to get 
Z b 
[ut(x, t)+ Fx(x, t) - g(x, t)] dx =0. a 
The above equation must hold for all intervals [a, b]. It follows that the integrand must be equal to zero, leading to the ‘di.erential form’ of the conservation law, 
ut + Fx = g(x, t). 
Many models in the sciences arise from this basic conservation argument. The next step is to determine the .ux f as as function of u and x (and the source). 

7.2 Deriving the heat equation 
If u is actually temperature, then the .ux can be modeled by Fourier’s law 
f = -aux 
where a is a constant (the thermal di.usivity, with units of m2/s). This simple law states that the the .ux of heat is towards cooler areas, and the rate is proportional not to the amount of heat but to the gradient in temperature, i.e. the heat will .ow faster if there is a large di.erence (e.g. an ice cube melting in a fridge vs. outside on a hot day). 
Thus if there is no external source of heat, then u satis.es the heat equation ut = auxx. 
More generally, if u is any quantity whose .ux is proportional to minus the gradient of u, then u will also satisfy the above. Such a process is called a di.usion process and the equation is then referred to as a di.usion equation. 



